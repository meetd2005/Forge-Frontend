# Frontend Environment Variables

# Google OAuth Configuration
# These are public client IDs that are safe to expose in the frontend
# Get these from your Google Cloud Console OAuth 2.0 Client IDs
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here

# API Environment Override (optional)
# Can be: development, staging, production
# If not set, will auto-detect based on domain
NEXT_PUBLIC_API_ENV=development

# Backend API URLs
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_POSTS_API_URL=http://localhost:8001
